# 功能测试用例管理模块 - 开发任务列表

> **文档版本**: v1.0
> **创建日期**: 2025-01-30
> **适用阶段**: MVP版本 (4-6周)
> **参考文档**:
> - 产品需求文档 → `功能测试模块-产品需求文档.md`
> - 技术实现方案 → `功能测试模块-技术实现方案.md`
> - 实施指南 → `功能测试模块-实施指南.md`

---

## 📋 任务概览

### 开发目标

构建一个**AI驱动的功能测试用例管理平台**,实现:
1. AI辅助需求澄清 (多轮对话,基于LangGraph状态图)
2. 自动生成测试点和测试用例
3. 表格/思维导图双视图展示
4. 多格式导出 (CSV/Excel/Markdown/PDF)
5. 历史用例知识库检索

### 技术栈

| 层级 | 技术选型 | 文档参考 |
|------|----------|----------|
| **AI编排** | **LangChain + LangGraph** ⭐ | [技术方案 §4.1 - 为什么需要LangGraph](功能测试模块-技术实现方案.md#41-为什么需要langgraph) |
| **后端** | FastAPI + SQLModel + PostgreSQL + pgvector | [技术方案 §1.2 - 技术栈选型](功能测试模块-技术实现方案.md#12-技术栈选型) |
| **前端** | React 19 + TypeScript + ReactFlow + shadcn/ui | [技术方案 §5.1 - 前端架构](功能测试模块-技术实现方案.md#51-前端架构) |

### 开发周期

- **总周期**: 6周 (MVP版本)
- **人员配置**: 1后端 + 1前端 + 1AI工程师(兼职)

---

## 🎯 Week 1: 数据库与基础架构 (后端)

### 任务1.1: 安装pgvector扩展 (0.5天)

**描述**: 在PostgreSQL中安装pgvector扩展,支持向量存储

**操作步骤**:
```bash
# 1. 连接到数据库
docker exec -it sisyphus-postgres-1 psql -U postgres -d sisyphus

# 2. 安装扩展
CREATE EXTENSION IF NOT EXISTS vector;

# 3. 验证安装
SELECT * FROM pg_extension WHERE extname = 'vector';
```

**验收标准**:
- ✅ pgvector扩展成功安装
- ✅ 可创建vector类型字段

**文档参考**: [技术方案 §3.1 - 数据库扩展安装](功能测试模块-技术实现方案.md#31-数据库扩展安装)

---

### 任务1.2: 创建核心数据表 (1.5天)

**描述**: 创建功能测试模块所需的所有数据表

**数据表清单**:
1. `ai_provider_configs` - AI厂商配置
2. `requirements` - 需求表
3. `ai_conversations` - AI对话历史
4. `test_points` - 测试点表
5. `test_cases` - 测试用例表
6. `test_case_knowledge` - 用例知识库 (向量)
7. `test_case_templates` - 用例模板
8. `file_attachments` - 文件附件

**操作步骤**:
1. 创建Alembic迁移脚本
   ```bash
   cd backend
   alembic revision --autogenerate -m "Add functional test module tables"
   ```

2. 编写迁移SQL (参考技术方案§3.2)

3. 执行迁移
   ```bash
   alembic upgrade head
   ```

**验收标准**:
- ✅ 所有表创建成功
- ✅ 向量索引创建成功
- ✅ 外键约束生效

**文档参考**:
- [技术方案 §3.2 - 核心数据表设计](功能测试模块-技术实现方案.md#32-核心数据表设计)
- [产品需求 §3.2.1 - AI配置管理数据模型](功能测试模块-产品需求文档.md#321-ai配置管理)

**关键SQL示例** (技术方案§3.2.6):
```sql
-- 向量存储示例
CREATE TABLE test_case_knowledge (
    id SERIAL PRIMARY KEY,
    test_case_id INTEGER REFERENCES test_cases(id),
    embedding vector(1536),  -- OpenAI: 1536维
    -- ...
);

-- 向量索引
CREATE INDEX idx_knowledge_embedding
ON test_case_knowledge
USING ivfflat (embedding vector_cosine_ops)
WITH (lists = 100);
```

---

### 任务1.3: 创建SQLModel数据模型 (1天)

**描述**: 在FastAPI中创建对应的数据模型

**文件结构**:
```
backend/app/models/
├── __init__.py
├── ai_config.py           # AI配置模型
├── requirement.py         # 需求模型
├── ai_conversation.py     # 对话历史模型
├── test_point.py          # 测试点模型
├── test_case.py           # 测试用例模型
└── test_case_knowledge.py # 知识库模型
```

**核心模型示例** (技术方案§3.2.2):
```python
# backend/app/models/requirement.py
from sqlmodel import SQLModel, Field, Column
from typing import Optional, List
from datetime import datetime

class Requirement(SQLModel, table=True):
    __tablename__ = "requirements"

    id: int = Field(primary_key=True)
    requirement_id: str = Field(unique=True, index=True)
    name: str
    module_id: Optional[str]
    module_name: str
    priority: str  # high/medium/low

    # 需求内容
    description: str  # Markdown格式
    attachments: List[str]  # MinIO存储路径

    # AI澄清记录
    ai_conversation_id: Optional[str]
    clarification_status: str  # draft/clarifying/confirmed
    risk_points: List[str]

    # 状态
    status: str  # draft/review/approved
    created_by: int
    created_at: datetime = Field(default_factory=datetime.utcnow)
```

**验收标准**:
- ✅ 所有模型类创建完成
- ✅ 字段类型与数据库表一致
- ✅ Pydantic验证正常工作

**文档参考**: [产品需求 §3.2 - 核心功能数据模型](功能测试模块-产品需求文档.md#32-核心功能详细设计)

---

### 任务1.4: 安装AI相关依赖 (0.5天)

**描述**: 安装LangChain、LangGraph、pgvector等依赖

**依赖清单**:
```
# backend/requirements.txt (新增)
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.10
langgraph>=0.0.20
pgvector>=0.2.0
```

**安装命令**:
```bash
pip install langchain langchain-openai langchain-community langgraph pgvector
```

**验收标准**:
- ✅ 所有依赖安装成功
- ✅ 可成功导入LangGraph

**文档参考**: [技术方案 §4.2 - 架构设计](功能测试模块-技术实现方案.md#42-langchain--langgraph架构设计)

---

### 任务1.5: 配置PostgreSQL Checkpointer (0.5天)

**描述**: 配置LangGraph的状态持久化,使用PostgreSQL作为存储后端

**配置文件**:
```python
# backend/app/services/ai/checkpointer.py
from langgraph.checkpoint.postgres import PostgresSaver

# 创建checkpointer
checkpointer = PostgresSaver.from_conn_string(
    "postgresql+asyncpg://postgres:password@localhost:5432/sisyphus"
)

# 初始化表 (首次运行)
await checkpointer.setup()
```

**验收标准**:
- ✅ checkpointer配置成功
- ✅ 状态可持久化到PostgreSQL

**文档参考**: [技术方案 §4.1 - LangGraph优势](功能测试模块-技术实现方案.md#41-为什么需要langgraph)

---

### Week 1 总结

**交付物**:
- ✅ 数据库表完整创建
- ✅ SQLModel模型完成
- ✅ AI依赖安装完成
- ✅ 状态持久化配置完成

**风险点**:
- ⚠️ pgvector扩展可能需要手动编译,预留额外时间

---

## 🎯 Week 2-3: 后端API + LangGraph状态图 (后端)

### 任务2.1: 实现AI服务基类 (0.5天)

**描述**: 创建AI服务基类,支持多厂商LLM

**文件**: `backend/app/services/ai/base.py`

**核心功能**:
- API Key加密/解密
- LLM实例获取
- Embedding实例获取

**代码示例** (技术方案§4.3.1):
```python
from abc import ABC, abstractmethod

class AIServiceBase(ABC):
    def __init__(self, provider_config: dict):
        self.provider_config = provider_config

    @abstractmethod
    async def get_llm(self):
        pass

    @abstractmethod
    async def get_embeddings(self):
        pass

    def encrypt_api_key(self, api_key: str) -> str:
        from cryptography.fernet import Fernet
        # 实现加密
        pass
```

**验收标准**:
- ✅ 基类创建完成
- ✅ 加密/解密功能正常

**文档参考**: [技术方案 §4.3 - 核心代码实现](功能测试模块-技术实现方案.md#43-核心代码实现)

---

### 任务2.2: 实现多厂商LLM服务 (1天)

**描述**: 实现支持OpenAI、通义千问、文心一言等多厂商的LLM服务

**文件**: `backend/app/services/ai/llm_service.py`

**支持厂商**:
- OpenAI (GPT-4, GPT-3.5)
- Anthropic (Claude 3 Opus/Sonnet)
- 阿里云 (通义千问)
- 百度 (文心一言)

**核心代码** (技术方案§4.2.2):
```python
from langchain_openai import ChatOpenAI
from langchain_anthropic import ChatAnthropic

class MultiVendorLLMService(AIServiceBase):
    async def get_llm(self):
        provider_type = self.provider_config["provider_type"]

        if provider_type == "openai":
            return ChatOpenAI(
                model=self.provider_config["model_name"],
                openai_api_key=self.decrypt_api_key(...),
            )
        elif provider_type == "anthropic":
            return ChatAnthropic(...)
        # ... 更多厂商
```

**验收标准**:
- ✅ 支持至少2家AI厂商 (OpenAI + 通义千问)
- ✅ API Key加密存储
- ✅ 可正常调用LLM

**文档参考**:
- [技术方案 §4.2.2 - 多厂商LLM支持](功能测试模块-技术实现方案.md#422-多厂商llm支持)
- [产品需求 §3.2.1 - AI配置管理](功能测试模块-产品需求文档.md#321-ai配置管理)

---

### 任务2.3: 实现向量化服务 (0.5天)

**描述**: 实现文本向量化,支持知识库检索

**文件**: `backend/app/services/ai/embedding_service.py`

**功能**:
- 单个文本向量化
- 批量向量化
- 支持OpenAI Embeddings和开源模型

**代码示例** (技术方案§4.2.3):
```python
from langchain_openai import OpenAIEmbeddings

class EmbeddingService(AIServiceBase):
    async def get_embeddings(self):
        return OpenAIEmbeddings(
            model="text-embedding-3-small",
            openai_api_key=self.decrypt_api_key(...),
        )

    async def embed_text(self, text: str) -> List[float]:
        embeddings = await self.get_embeddings()
        return await embeddings.aembed_query(text)
```

**验收标准**:
- ✅ 可成功向量化文本
- ✅ 返回1536维向量 (OpenAI)

**文档参考**: [技术方案 §4.2.3 - 向量化服务](功能测试模块-技术实现方案.md#423-向量化服务)

---

### 任务2.4: 实现需求澄清LangGraph状态图 ⭐ (2天)

**这是整个项目的核心功能!**

**文件**: `backend/app/services/ai/graphs/requirement_clarification_graph.py`

**核心功能**:
1. **定义状态结构** - 包含需求文档、风险点、问题列表等
2. **实现节点函数** - AI分析、提出问题、更新需求、生成报告
3. **实现条件边** - 决定是继续提问还是结束
4. **人工干预节点** - 暂停等待用户输入

**状态定义** (技术方案§4.3):
```python
from typing import TypedDict, Annotated, List
from langgraph.graph import StateGraph
from langchain_core.messages import BaseMessage

class RequirementClarificationState(TypedDict):
    # 对话历史
    messages: Annotated[List[BaseMessage], operator.add]

    # 需求相关
    requirement_id: int
    original_requirement: str
    current_requirement: str

    # 澄清过程
    questions_asked: List[str]
    questions_remaining: List[str]
    identified_gaps: List[str]
    identified_risks: List[str]

    # 流程控制
    clarification_complete: bool
    iteration_count: int
```

**状态图流程**:
```
开始 → analyze_requirement (AI分析需求)
   ↓
should_continue? (有缺失信息?)
   ├─ Yes → ask_question (提出问题)
   │        ↓
   │     human_input (等待用户输入) ← 前端暂停
   │        ↓
   │     update_requirement (更新需求文档)
   │        ↓
   │     循环回 analyze_requirement
   │
   └─ No → generate_report (生成最终报告)
           ↓
         结束
```

**节点函数示例**:
```python
async def analyze_requirement_node(state: RequirementClarificationState):
    """AI分析需求,识别缺失信息"""
    prompt = ChatPromptTemplate.from_messages([
        ("system", "你是一位资深产品经理..."),
        ("user", "当前需求:\n{current_requirement}")
    ])

    chain = prompt | llm
    response = await chain.ainvoke({"current_requirement": state["current_requirement"]})

    result = json.loads(response.content)

    return {
        **state,
        "identified_gaps": result["identified_gaps"],
        "questions_remaining": result["questions_to_ask"],
        "identified_risks": result["identified_risks"],
    }
```

**API接口**:
```python
# backend/app/api/v1/endpoints/ai_requirement.py

@router.post("/start-clarification")
async def start_clarification(request: StartRequest):
    """开始需求澄清,返回thread_id"""
    app = create_requirement_clarification_graph()

    # 初始化状态
    initial_state = {
        "messages": [],
        "requirement_id": request.requirement_id,
        "original_requirement": request.requirement_text,
        # ...
    }

    # 执行图 (会停在human_input节点)
    config = {"configurable": {"thread_id": f"req_{request.requirement_id}"}}
    result = await app.ainvoke(initial_state, config)

    return {"thread_id": config["configurable"]["thread_id"], "ai_message": result["messages"][-1]}

@router.post("/continue-clarification")
async def continue_clarification(request: ContinueRequest):
    """用户回复后继续执行"""
    app = create_requirement_clarification_graph()
    config = {"configurable": {"thread_id": request.thread_id}}

    # 添加用户消息,继续执行
    result = await app.ainvoke(
        {"messages": [HumanMessage(content=request.user_answer)]},
        config,
    )

    return {"ai_message": result["messages"][-1], "clarification_complete": result["clarification_complete"]}
```

**验收标准**:
- ✅ 状态图可正常执行
- ✅ 在human_input节点正确暂停
- ✅ 用户回复后可继续执行
- ✅ 状态持久化到PostgreSQL
- ✅ 迭代次数达到上限时自动结束

**文档参考**:
- [技术方案 §4.3 - LangGraph实现](功能测试模块-技术实现方案.md#43-核心代码实现)
- [产品需求 §3.2.2 - AI需求澄清界面](功能测试模块-产品需求文档.md#322-需求管理)

**关键提示**:
- 使用PostgreSQL checkpointer实现状态持久化
- 前端通过SSE或WebSocket接收AI响应
- 人工干预节点在前端实现 (暂停-恢复机制)

---

### 任务2.5: 实现测试点生成功能 (1天)

**描述**: 基于需求文档生成测试点

**文件**: `backend/app/services/ai/graphs/test_point_generation_graph.py`

**核心功能**:
1. 检索相关历史用例 (向量搜索)
2. 基于需求文档生成测试点
3. 覆盖功能/性能/安全/兼容性等多个维度
4. 保存测试点到数据库

**代码结构**:
```python
class TestPointGenerationChain:
    async def agenerate_test_points(self, requirement_text: str, requirement_id: int):
        # 1. 检索相关历史用例
        related_cases = await vector_store_service.similarity_search(
            query=requirement_text,
            k=5,
        )

        # 2. 生成测试点
        prompt = PromptTemplate.from_template(...)
        chain = prompt | llm
        response = await chain.ainvoke({
            "requirement_text": requirement_text,
            "related_test_cases": related_cases,
        })

        # 3. 解析并保存
        test_points = json.loads(response)
        for tp in test_points["test_points"]:
            await save_test_point(tp)

        return test_points
```

**API接口**:
```python
@router.post("/generate-test-points")
async def generate_test_points(requirement_id: int):
    requirement = await session.get(Requirement, requirement_id)

    test_points = await chain.agenerate_test_points(
        requirement_text=requirement.description,
        requirement_id=requirement_id,
    )

    return {"test_points": test_points}
```

**验收标准**:
- ✅ 可生成测试点
- ✅ 测试点覆盖多个维度
- ✅ 保存到数据库成功

**文档参考**:
- [技术方案 §4.2.6 - 测试点生成链](功能测试模块-技术实现方案.md#426-测试点生成链)
- [产品需求 §3.2.3 - 测试点管理](功能测试模块-产品需求文档.md#323-测试点管理)

---

### 任务2.6: 实现测试用例生成功能 (1.5天)

**描述**: 基于测试点生成详细的测试用例

**文件**: `backend/app/services/ai/graphs/test_case_generation_graph.py`

**核心功能**:
1. 基于测试点生成详细用例
2. 包含操作步骤和预期结果
3. 按【模块-页面】维度组织
4. 保存到数据库并添加到知识库

**代码示例** (技术方案§4.2.7):
```python
async def agenerate_test_cases(
    self,
    requirement_text: str,
    test_points: List[Dict],
    module_page_mapping: Dict[str, str],
):
    # 1. 格式化测试点
    test_points_text = "\n".join([
        f"- {tp['title']} (优先级: {tp['priority']})"
        for tp in test_points
    ])

    # 2. 生成用例
    prompt = self.get_prompt_template()
    chain = prompt | llm
    response = await chain.ainvoke({
        "requirement_text": requirement_text,
        "test_points": test_points_text,
    })

    # 3. 解析并保存
    test_cases = json.loads(response)["test_cases"]
    for tc in test_cases:
        tc["module_name"] = module_page_mapping.get("module")
        tc["page_name"] = module_page_mapping.get("page")
        await save_test_case(tc)
        await add_to_knowledge_base(tc)  # 向量化

    return test_cases
```

**API接口**:
```python
@router.post("/generate-test-cases")
async def generate_test_cases(
    requirement_id: int,
    module_page_mapping: Dict[str, str],  # 前端传入
):
    test_cases = await chain.agenerate_test_cases(
        requirement_text=requirement.description,
        test_points=requirement.test_points,
        module_page_mapping=module_page_mapping,
    )

    return {"test_cases": test_cases}
```

**验收标准**:
- ✅ 生成详细测试用例
- ✅ 操作步骤与预期结果一一对应
- ✅ 向量化并添加到知识库

**文档参考**:
- [技术方案 §4.2.7 - 测试用例生成链](功能测试模块-技术实现方案.md#427-测试用例生成链)
- [产品需求 §3.2.4 - 测试用例管理](功能测试模块-产品需求文档.md#324-测试用例管理)

---

### 任务2.7: 实现向量检索服务 (1天)

**描述**: 实现知识库向量检索,支持语义搜索

**文件**: `backend/app/services/ai/vector_store_service.py`

**核心功能**:
1. 将测试用例向量化
2. 存储到pgvector
3. 语义相似度搜索
4. 支持元数据过滤

**代码示例** (技术方案§4.2.4):
```python
from langchain.vectorstores import PGVector

class VectorStoreService:
    async def similarity_search(self, query: str, k: int = 5, filter: dict = None):
        vector_store = await self.get_vector_store()
        return await vector_store.asimilarity_search(
            query=query,
            k=k,
            filter=filter,  # {"module_name": "用户管理"}
        )

    async def add_test_case_to_knowledge(self, test_case: TestCase):
        # 1. 构建文本
        text_content = f"用例:{test_case.title}\n步骤:{...}"

        # 2. 向量化
        embedding = await self.embedding_service.embed_text(text_content)

        # 3. 存储
        knowledge = TestCaseKnowledge(
            test_case_id=test_case.id,
            embedding=embedding,
            module_name=test_case.module_name,
            ...
        )
        session.add(knowledge)
        await session.commit()
```

**验收标准**:
- ✅ 可向量化测试用例
- ✅ 语义搜索准确度 > 80%
- ✅ 元数据过滤正常工作

**文档参考**: [技术方案 §4.2.4 - 向量检索服务](功能测试模块-技术实现方案.md#424-向量检索服务)

---

### 任务2.8: 实现AI配置管理API (1天)

**描述**: 实现AI厂商配置的CRUD接口

**API接口**:
- `POST /api/v1/ai/configs` - 添加AI配置
- `GET /api/v1/ai/configs` - 获取所有配置
- `PUT /api/v1/ai/configs/{id}` - 更新配置
- `DELETE /api/v1/ai/configs/{id}` - 删除配置
- `POST /api/v1/ai/configs/{id}/set-default` - 设为默认

**核心功能**:
- API Key加密存储
- 支持多种AI厂商
- 脱敏显示 (只显示前4位和后4位)

**验收标准**:
- ✅ 所有CRUD接口正常
- ✅ API Key加密存储
- ✅ 脱敏显示正确

**文档参考**: [产品需求 §3.2.1 - AI配置管理](功能测试模块-产品需求文档.md#321-ai配置管理)

---

### Week 2-3 总结

**交付物**:
- ✅ LangGraph需求澄清状态图完整实现
- ✅ 测试点/用例生成功能完成
- ✅ 向量检索服务完成
- ✅ AI配置管理API完成

**关键里程碑**:
- 🎯 核心AI功能 (需求澄清) 实现完成

---

## 🎯 Week 3-4: 前端页面开发 (前端)

### 任务3.1: 创建AI对话组件 ⭐ (2天)

**这是最复杂的前端组件,需要实现与LangGraph的交互!**

**文件**: `frontend/src/pages/functional-test/components/AIClarificationChat.tsx`

**核心功能**:
1. 显示AI消息和用户消息
2. 实时流式输出AI响应
3. 暂停机制 (等待用户输入)
4. 支持Markdown渲染
5. 风险点高亮显示

**代码结构** (技术方案§5.2.1):
```typescript
interface Message {
  role: 'user' | 'assistant'
  content: string
  timestamp: Date
}

export function AIClarificationChat({ requirementId }: Props) {
  const [messages, setMessages] = useState<Message[]>([])
  const [threadId, setThreadId] = useState<string | null>(null)
  const [isLoading, setIsLoading] = useState(false)

  // 开始澄清
  const { mutate: startClarification } = useMutation({
    mutationFn: async () => {
      return await api.startRequirementClarification({
        requirement_id: requirementId,
        original_requirement: initialContent,
      })
    },
    onSuccess: (data) => {
      setThreadId(data.thread_id)
      setMessages([{ role: 'assistant', content: data.ai_message }])
    },
  })

  // 继续澄清 (用户回复)
  const { mutate: continueClarification } = useMutation({
    mutationFn: async (userAnswer: string) => {
      return await api.continueRequirementClarification({
        thread_id: threadId!,
        user_answer: userAnswer,
      })
    },
    onSuccess: (data) => {
      setMessages([
        ...messages,
        { role: 'user', content: input },
        { role: 'assistant', content: data.ai_message },
      ])

      if (data.clarification_complete) {
        onUpdateRequirement(data.current_requirement)
      }
    },
  })

  return (
    <div className="flex h-[600px] flex-col">
      {/* 消息列表 */}
      <ScrollArea className="flex-1 p-4">
        {messages.map((msg, idx) => (
          <div key={idx} className={msg.role === 'user' ? 'text-right' : 'text-left'}>
            <div className={msg.role === 'user' ? 'bg-blue-500 text-white' : 'bg-gray-100'}>
              <Markdown>{msg.content}</Markdown>
            </div>
          </div>
        ))}
        {isLoading && <LoadingSpinner />}
      </ScrollArea>

      {/* 输入框 */}
      <div className="border-t p-4">
        <Textarea
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyDown={(e) => {
            if (e.key === 'Enter' && !e.shiftKey) {
              e.preventDefault()
              continueClarification(input)
            }
          }}
          placeholder="输入你的回复... (Enter发送)"
          rows={3}
        />
      </div>
    </div>
  )
}
```

**交互流程**:
```
用户点击"开始AI澄清"
  → 调用 startClarification()
  → 返回 thread_id + AI第一次问题
  → 显示AI消息

用户输入回复
  → 调用 continueClarification(user_answer)
  → 传递 thread_id + user_answer
  → LangGraph继续执行
  → 返回AI下一步响应
  → 循环...
```

**验收标准**:
- ✅ AI消息流式输出
- ✅ 支持Markdown渲染
- ✅ Enter发送, Shift+Enter换行
- ✅ 风险点高亮显示

**文档参考**:
- [技术方案 §5.2.1 - AI对话组件](功能测试模块-技术实现方案.md#521-ai对话组件)
- [产品需求 §3.2.2 - AI需求澄清界面](功能测试模块-产品需求文档.md#322-需求管理)

---

### 任务3.2: 创建需求管理页面 (1天)

**文件**: `frontend/src/pages/functional-test/RequirementManagement.tsx`

**核心功能**:
1. 需求列表 (支持搜索、筛选)
2. 创建/编辑需求表单
3. 上传附件 (图片、文件)
4. 启动AI澄清按钮
5. 需求文档实时预览

**界面参考**: [产品需求 §3.2.2 - 创建需求流程](功能测试模块-产品需求文档.md#322-需求管理)

**验收标准**:
- ✅ 需求CRUD正常
- ✅ 文件上传到MinIO成功
- ✅ AI澄清按钮正确调用接口

---

### 任务3.3: 创建测试点列表组件 (0.5天)

**文件**: `frontend/src/pages/functional-test/components/TestPointList.tsx`

**核心功能**:
1. 显示AI生成的测试点
2. 按分类分组 (功能/性能/安全/兼容性)
3. 编辑/删除测试点
4. 手动添加测试点
5. 覆盖率分析可视化

**界面参考**: [产品需求 §3.2.3 - 测试点管理界面](功能测试模块-产品需求文档.md#323-测试点管理)

**验收标准**:
- ✅ 测试点展示清晰
- ✅ 支持编辑和删除
- ✅ 覆盖率分析图表正确

---

### 任务3.4: 创建测试用例表格视图 (1.5天)

**文件**: `frontend/src/pages/functional-test/components/TestCaseTable.tsx`

**核心功能**:
1. 按模块-页面分组展示用例
2. 显示操作步骤和预期结果 (表格形式)
3. 展开/折叠用例详情
4. 编辑/删除/复制用例
5. 批量操作

**界面参考**: [产品需求 §3.2.4 - 用例表格视图](功能测试模块-产品需求文档.md#324-测试用例管理)

**代码结构**:
```typescript
export function TestCaseTable({ testCases }: Props) {
  // 按模块分组
  const groupedCases = useMemo(() => {
    return testCases.reduce((acc, tc) => {
      if (!acc[tc.module_name]) acc[tc.module_name] = {}
      if (!acc[tc.module_name][tc.page_name]) acc[tc.module_name][tc.page_name] = []
      acc[tc.module_name][tc.page_name].push(tc)
      return acc
    }, {} as Record<string, Record<string, TestCase[]>>)
  }, [testCases])

  return (
    <div>
      {Object.entries(groupedCases).map(([moduleName, pages]) => (
        <ModuleGroup key={moduleName} name={moduleName}>
          {Object.entries(pages).map(([pageName, cases]) => (
            <PageGroup key={pageName} name={pageName} cases={cases} />
          ))}
        </ModuleGroup>
      ))}
    </div>
  )
}
```

**验收标准**:
- ✅ 表格视图与禅道格式一致
- ✅ 展开/折叠正常
- ✅ 批量操作功能正常

---

### 任务3.5: 创建思维导图组件 (1.5天)

**文件**: `frontend/src/pages/functional-test/components/MindMapViewer.tsx`

**核心功能**:
1. 使用ReactFlow渲染思维导图
2. 按需求 → 模块 → 页面 → 用例层级展示
3. 展开/折叠节点
4. 双击节点跳转到表格视图
5. 导出为PNG/SVG

**界面参考**: [产品需求 §3.2.5 - 思维导图视图](功能测试模块-产品需求文档.md#325-思维导图视图)

**代码示例** (技术方案§5.2.2):
```typescript
import ReactFlow, { Node, Edge } from 'reactflow'

export function MindMapViewer({ testCases }: Props) {
  // 构建节点和边
  const { nodes, edges } = useMemo(() => {
    // 根节点
    const nodes: Node[] = [{ id: 'root', data: { label: '需求名称' }, position: { x: 0, y: 0 } }]
    const edges: Edge[] = []

    // 按模块分组...
    return { nodes, edges }
  }, [testCases])

  return (
    <div className="h-[800px]">
      <ReactFlow nodes={nodes} edges={edges} fitView>
        <Controls />
        <MiniMap />
        <Background />
      </ReactFlow>
    </div>
  )
}
```

**验收标准**:
- ✅ 思维导图正确渲染
- ✅ 节点可展开/折叠
- ✅ 导出PNG成功

**文档参考**: [技术方案 §5.2.2 - 思维导图组件](功能测试模块-技术实现方案.md#522-思维导图组件)

---

### 任务3.6: 创建导出功能组件 (1天)

**文件**: `frontend/src/pages/functional-test/components/ExportDialog.tsx`

**核心功能**:
1. 选择导出格式 (CSV/Excel/Markdown/PDF)
2. CSV格式自动拼接需求ID和模块ID
3. 文件命名预览
4. 下载导出文件

**界面参考**: [产品需求 §3.2.6 - 导出界面](功能测试模块-产品需求文档.md#326-导出与集成)

**代码示例** (技术方案§5.2.3):
```typescript
export function ExportDialog({ testCases, requirement }: Props) {
  const [formats, setFormats] = useState({
    csv: true,
    excel: false,
    markdown: false,
    pdf: false,
  })

  const handleExport = async () => {
    for (const [format, enabled] of Object.entries(formats)) {
      if (enabled) {
        await api.exportTestCases({
          requirement_id: requirement.id,
          format,
          include_ids: true,
          include_module_id: true,
        })
        // 下载文件...
      }
    }
  }

  return (
    <Dialog>
      <Checkbox checked={formats.csv} onChange={(e) => setFormats({ ...formats, csv: e.target.checked })}>
        CSV (导入禅道)
      </Checkbox>
      {/* ... */}
    </Dialog>
  )
}
```

**验收标准**:
- ✅ CSV格式符合禅道导入要求
- ✅ 自动拼接需求ID和模块ID
- ✅ 其他格式导出正常

**文档参考**:
- [产品需求 §3.2.6 - CSV导出格式](功能测试模块-产品需求文档.md#326-导出与集成)
- [技术方案 §5.2.3 - 导出功能](功能测试模块-技术实现方案.md#523-导出功能组件)

---

### 任务3.7: 创建知识库页面 (0.5天)

**文件**: `frontend/src/pages/functional-test/KnowledgeBase.tsx`

**核心功能**:
1. 统计概览 (总用例数、覆盖率等)
2. 语义搜索
3. 用例模板管理
4. 相似用例推荐

**界面参考**: [产品需求 §3.2.7 - 知识库管理](功能测试模块-产品需求文档.md#327-知识库管理)

**验收标准**:
- ✅ 语义搜索准确
- ✅ 统计数据正确
- ✅ 用例模板可用

---

### Week 3-4 总结

**交付物**:
- ✅ AI对话组件完成 (核心功能)
- ✅ 需求管理页面完成
- ✅ 测试用例表格/思维导图完成
- ✅ 导出功能完成

---

## 🎯 Week 5: 联调测试 (全员)

### 任务4.1: 端到端测试 (2天)

**测试场景**:
1. **场景1: 完整的需求澄清流程**
   - 创建需求
   - 启动AI澄清
   - 多轮对话 (至少3轮)
   - 确认需求文档
   - 验证风险点识别

2. **场景2: 测试用例生成流程**
   - 基于需求生成测试点
   - 基于测试点生成用例
   - 验证用例格式
   - 验证知识库向量化

3. **场景3: 知识库检索**
   - 生成多条测试用例
   - 语义搜索历史用例
   - 验证搜索准确性

**验收标准**:
- ✅ 所有端到端场景通过
- ✅ AI响应时间 < 5s
- ✅ 无明显Bug

---

### 任务4.2: 性能优化 (1.5天)

**优化项**:
1. **数据库优化**
   - 向量索引参数调优
   - 连接池配置
   - 查询优化

2. **AI调用优化**
   - Token缓存
   - 批量向量化
   - 异步任务

3. **前端优化**
   - 虚拟滚动
   - 懒加载
   - 代码分割

**文档参考**: [技术方案 §七 - 性能优化方案](功能测试模块-技术实现方案.md#七-性能优化方案)

**验收标准**:
- ✅ 页面加载时间 < 2s
- ✅ AI响应时间 < 5s
- ✅ 向量检索 < 100ms

---

### 任务4.3: Bug修复 (1.5天)

**常见Bug预判**:
- LangGraph状态图死循环
- 向量检索结果不准确
- 前端Markdown渲染异常
- 文件上传失败
- API Key解密失败

**验收标准**:
- ✅ 所有P0/P1 Bug修复
- ✅ 回归测试通过

---

### Week 5 总结

**交付物**:
- ✅ 所有核心功能测试通过
- ✅ 性能指标达标
- ✅ 无严重Bug

---

## 🎯 Week 6: 部署与验收 (全员)

### 任务5.1: 编写部署文档 (0.5天)

**文档内容**:
1. 环境准备
2. Docker Compose部署
3. 数据库迁移
4. AI配置说明
5. 启动步骤

**文档参考**: [技术方案 §6 - 部署方案](功能测试模块-技术实现方案.md#六-部署方案)

---

### 任务5.2: Docker Compose部署 (0.5天)

**配置文件**: `docker-compose.yml`

**服务清单**:
- PostgreSQL (pgvector)
- Redis
- MinIO
- Backend (FastAPI)
- Frontend (Vite)

**验收标准**:
- ✅ 一键启动所有服务
- ✅ 所有服务正常运行

---

### 任务5.3: 用户验收测试 (2天)

**邀请用户**:
- 测试工程师
- 测试负责人
- 产品经理

**测试重点**:
1. AI澄清效果是否满足需求
2. 测试用例质量是否达标
3. 操作是否流畅
4. 是否有明显的体验问题

**收集反馈**:
- 功能建议
- Bug反馈
- 优化建议

**验收标准**:
- ✅ 用户满意度 ≥ 4.0/5.0
- ✅ 核心功能无阻塞性Bug

---

### 任务5.4: 编写用户手册 (1天)

**文档内容**:
1. 快速开始
2. 功能说明
3. 常见问题
4. 视频教程 (可选)

**文档输出**: `docs/用户手册.md`

---

### 任务5.5: MVP版本发布 (0.5天)

**发布清单**:
- [ ] 所有代码合并到main分支
- [ ] 版本号更新为v1.0.0
- [ ] Release Notes编写
- [ ] Docker镜像推送
- [ ] 部署到测试环境
- [ ] 通知所有用户

---

### Week 6 总结

**交付物**:
- ✅ 部署文档完成
- ✅ Docker Compose一键部署成功
- ✅ 用户验收通过
- ✅ MVP版本发布

---

## 📊 整体进度跟踪

### 关键里程碑

| 里程碑 | 完成标准 | 预计完成时间 |
|--------|----------|-------------|
| **M1: 数据库完成** | 所有表创建完成,向量索引生效 | Week 1 Day 3 |
| **M2: AI服务完成** | LangGraph状态图可运行 | Week 2 Day 5 |
| **M3: 后端API完成** | 所有核心接口可调用 | Week 3 Day 3 |
| **M4: 前端页面完成** | 核心页面可访问 | Week 4 Day 5 |
| **M5: 联调测试完成** | 端到端测试通过 | Week 5 Day 5 |
| **M6: MVP发布** | 部署上线,用户可用 | Week 6 Day 5 |

### 每日站会检查点

**每日必问**:
1. 今天完成了哪些任务?
2. 遇到了哪些阻碍?
3. 明天计划做什么?
4. 是否需要协助?

---

## ⚠️ 风险与应对

### 技术风险

| 风险 | 概率 | 影响 | 应对措施 | 预案 |
|------|------|------|----------|------|
| LangGraph学习曲线陡峭 | 中 | 高 | 提前学习官方文档,先做POC | 降级使用ConversationChain |
| 向量检索准确率低 | 中 | 中 | 测试多个Embedding模型 | 结合关键词检索 |
| AI生成质量不稳定 | 高 | 高 | 持续优化Prompt | 支持用户手动编辑 |
| pgvector性能瓶颈 | 低 | 中 | 优化索引参数 | 考虑Milvus |

### 进度风险

| 风险 | 应对措施 |
|------|----------|
| 前后端进度不一致 | Week 3开始前后端联调,每天同步 |
| AI需求澄清功能开发超时 | 优先实现核心功能,流式输出可后续优化 |
| 测试用例生成质量不达标 | 保留人工编辑入口,不完全依赖AI |

---

## 📚 参考文档索引

### 产品相关

| 文档 | 路径 | 关键章节 |
|------|------|----------|
| 产品需求文档 | `功能测试模块-产品需求文档.md` | §3.2 核心功能详细设计 |
| | | §3.2.1 AI配置管理 |
| | | §3.2.2 需求管理 (AI澄清界面) |
| | | §3.2.3 测试点管理 |
| | | §3.2.4 测试用例管理 |
| | | §3.2.5 思维导图视图 |
| | | §3.2.6 导出与集成 |

### 技术相关

| 文档 | 路径 | 关键章节 |
|------|------|----------|
| 技术实现方案 | `功能测试模块-技术实现方案.md` | §1.2 技术栈选型 |
| | | §3.2 核心数据表设计 |
| | | §4.1 为什么需要LangGraph |
| | | §4.2 LangChain + LangGraph架构 |
| | | §4.3 核心代码实现 |
| | | §5.1 前端架构 |
| | | §5.2 核心组件实现 |
| | | §6 部署方案 |

### 实施相关

| 文档 | 路径 | 关键章节 |
|------|------|----------|
| 实施指南 | `功能测试模块-实施指南.md` | §架构选型决策 |
| | | §实施路线图 |
| | | §成本估算 |
| | | §快速开始 |

---

## ✅ 验收标准总结

### 功能验收

- [ ] AI配置管理 - 支持OpenAI和通义千问
- [ ] 需求澄清 - 多轮对话,状态持久化
- [ ] 测试点生成 - 覆盖4个维度以上
- [ ] 测试用例生成 - 包含步骤和预期结果
- [ ] 表格视图 - 类似禅道格式
- [ ] 思维导图视图 - 可展开/折叠
- [ ] 导出功能 - CSV/Excel至少2种格式
- [ ] 知识库检索 - 语义搜索准确

### 性能验收

- [ ] 首屏加载时间 < 2s
- [ ] AI响应时间 < 5s
- [ ] 向量检索 < 100ms
- [ ] 50条用例导出 < 10s

### 质量验收

- [ ] AI生成准确率 ≥ 85%
- [ ] 代码测试覆盖率 ≥ 70%
- [ ] 无P0/P1级Bug

---

## 🎯 后续规划 (v2.0)

MVP完成后,可考虑以下功能:

1. **思维导图完整版** (Week 7-8)
2. **PDF导出** (Week 8)
3. **多AI厂商支持** (Week 9)
4. **知识库高级检索** (Week 10)
5. **用例评审协作** (Week 11-12)

详见: [产品需求 §7.2 - 完整版本](功能测试模块-产品需求文档.md#72-完整版本--全功能)

---

**文档版本**: v1.0
**最后更新**: 2025-01-30
**维护者**: 开发团队
